import type { Metadata } from "next"
export const metadata: Metadata = {
  title: "Environment Generator",
  description: "Generate a custom Dockerfile, docker-compose.yml, and .env for your project in seconds. Supports 300+ language + framework + database combinations.",
  openGraph: { title: "Environment Generator | EnvSetup.dev", description: "Generate production-ready dev environments for any stack." },
}
